<h1>My Tasks page #index</h1>

  <ul>
    <% @tasks.each do |task| %>
    <li> <!-- addcheckbox -->
      <%= task.completed ? '[X]':'[ ]' %>
      <%= link_to task.title, task_path(task) %>
      <%= link_to "Edit", edit_task_path(task) %>
    </li>
    <% end %>
  </ul>
